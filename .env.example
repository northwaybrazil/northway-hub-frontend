# Optional. Leave empty in production so Render can rewrite /api/*.
# VITE_API_URL=http://localhost:8000

# Optional. Set to true in local dev to disable fake login/mock mode.
# VITE_DISABLE_LOCAL_MOCK=true
